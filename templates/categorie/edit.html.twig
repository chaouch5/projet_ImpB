{% extends 'base.html.twig' %}
    {% block body %}
    
    <div style="position : absolute;margin-left:17%;width:83%; height: auto;">  <!-- jquery validation -->
        <!-- jquery validation -->
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Modifier une catégorie</h3>
                    {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
                </div>
                <!-- /.card-header -->
                <!-- form start -->
           <form id="quickForm" novalidate="novalidate">
                    <div class="card-body">
                        <div class="form-group">
                            <label for="exampleInputEmail1">Nom de catégorie</label>
                            {{ form_widget(form.nomC,{'attr':{'class':'form-control'}}) }}
                        </div>
                         <!-- /.card-body -->
            <div class="card-footer">
                {{ form_widget(form.Valider,{'attr':{'class':'btn btn-warning'}}) }}
                 <input class="btn btn-secondary" type = "button" value = "Retour!"  onclick = "history.back()">
            </div>
                    {{ form_end(form) }}
                </form>
            </div>
            <!-- /.card -->
        </div>
    {% endblock %}